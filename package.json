{
  "name": "@itunified.io/mcp-proxctl",
  "version": "2026.4.11-4",
  "description": "MCP server for proxctl — 34 tools wrapping the proxctl Proxmox provisioning CLI via execFile",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mcp-proxctl": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "prepublishOnly": "node scripts/prepublish-check.js",
    "start": "node dist/index.js",
    "start:sse": "node dist/index.js --sse"
  },
  "keywords": ["mcp", "proxmox", "proxctl", "provisioning", "vm", "kickstart", "ai"],
  "author": "ITUNIFIED GmbH",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/itunified-io/mcp-proxctl.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
